# Threads API 자격 증명 (Meta for Developers 에서 발급)
# GitHub Actions 사용 시에는 Repository Secrets 에 등록합니다.
THREADS_USER_ID="your_threads_user_id"
THREADS_ACCESS_TOKEN="your_long_lived_threads_access_token"

# 실행 모드 설정 (선택 사항)
# DRY_RUN=true 로 설정하면 실제 포스팅 API를 호출하지 않고 시뮬레이션만 진행합니다.
DRY_RUN=false
